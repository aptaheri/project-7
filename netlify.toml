[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# API routes live in public/_redirects, which Netlify processes before this
# file — rules here would lose to that file's SPA catch-all.
